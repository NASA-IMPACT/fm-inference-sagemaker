{
  "name": "stress_test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "generate_report:rest": "source env.sh && TARGET_PATH=${TARGET_PATH} artillery run ${TARGET_FOLDER}/config.yaml --output preloaded_events.json",
    "generate_report:temporal": "source env.sh  &&  TARGET_PATH=${TARGET_PATH_QUEUE} artillery run test_flood_inference/config.yaml --output temporal_report.json",
    "generate_ui_report:rest": "source env.sh  && TARGET_PATH=${TARGET_PATH_REST} artillery run test_flood_inference/config.yaml --name \"service-flood-rest-$(date +%x_%r)\" --record --key ${ARTILLERY_API_KEY}",
    "generate_ui_report:queue": "source env.sh  && TARGET_PATH=${TARGET_PATH_QUEUE} artillery run test_flood_inference/config.yaml --output floods_queue_events.json --name \"service-flood-queue-dgx-$(date +%x_%r)\"  --record --key ${ARTILLERY_API_KEY}"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "artillery": "^2.0.29"
  }
}
