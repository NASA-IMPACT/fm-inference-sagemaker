apiVersion: v1
kind: Service
metadata:
  name: {{ include "fm-inference-sagemaker.fullname" . }}
  labels:
    {{- include "fm-inference-sagemaker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fm-inference-sagemaker.selectorLabels" . | nindent 4 }}