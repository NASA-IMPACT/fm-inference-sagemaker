apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prediction-ingress
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    argocd.argoproj.io/health-check: |
      hs = {}
      hs.status = "Healthy"
      return hs
spec:
  ingressClassName: traefik
  rules:
  - host: {{ .Values.hostServices }}
    http:
      paths:
      - path: {{ .Values.ingressSunTiler.path }}
        pathType: Prefix
        backend:
          service:
            name: tile-service
            port:
              number: 80
      - path: {{ .Values.ingressPrediction.path }}
        pathType: Prefix
        backend:
          service:
            name: prediction-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-services-ingress
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  rules:
  - host: {{ .Values.hostServices }}
    http:
      paths:
      - path: {{ .Values.ingresGoServices.path }}
        pathType: Prefix
        backend:
          service:
            name: go-services-service
            port:
              number: 80
---
